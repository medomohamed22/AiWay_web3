import {allowMethods,db,handleError,json,packageQuote,requireUser} from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['POST']))return;try{const user=await requireUser(req);const {paymentId,packageId}=req.body||{},quote=await packageQuote(packageId);if(!paymentId||!quote)return json(res,400,{error:'Invalid package'});const response=await fetch(`https://api.minepi.com/v2/payments/${encodeURIComponent(paymentId)}/approve`,{method:'POST',headers:{Authorization:`Key ${process.env.PI_SECRET_KEY}`,'Content-Type':'application/json'}}),data=await response.json().catch(()=>null);if(!response.ok)return json(res,response.status,{error:data||'Pi approval failed'});const {error}=await db().from('payments').upsert({user_id:user.id,payment_id:paymentId,package_id:packageId,amount_pi:quote.amountPi,usd_amount:quote.usd,pi_usd_rate:quote.piUsd,ai_tokens:quote.tokens,status:'approved',raw_response:data},{onConflict:'payment_id'});if(error)throw error;return json(res,200,{approved:true,amountPi:quote.amountPi})}catch(e){return handleError(e,res,'Unable to approve payment')}}
