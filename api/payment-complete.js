import {allowMethods,db,handleError,json,requireUser} from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['POST']))return;try{const user=await requireUser(req);const {paymentId,txid,packageId}=req.body||{};if(!paymentId||!txid||!packageId)return json(res,400,{error:'Invalid payment'});const s=db(),{data:p}=await s.from('payments').select('*').eq('payment_id',paymentId).eq('user_id',user.id).single();if(p?.status==='completed')return json(res,200,{completed:true,tokens:p.ai_tokens,alreadyCompleted:true});if(!p||p.package_id!==packageId)return json(res,400,{error:'Payment was not approved'});const response=await fetch(`https://api.minepi.com/v2/payments/${encodeURIComponent(paymentId)}/complete`,{method:'POST',headers:{Authorization:`Key ${process.env.PI_SECRET_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({txid})}),data=await response.json().catch(()=>null);if(!response.ok)return json(res,response.status,{error:data||'Pi completion failed'});const {error}=await s.rpc('complete_token_purchase',{p_user_id:user.id,p_payment_id:paymentId,p_txid:txid,p_tokens:p.ai_tokens,p_raw:data});if(error)throw error;return json(res,200,{completed:true,tokens:p.ai_tokens})}catch(e){return handleError(e,res,'Unable to complete payment')}}
